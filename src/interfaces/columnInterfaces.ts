export type createColumn = {
  title: string;
  order: number;
};

export type updateColumn = {
  id: string;
  title: string;
  order: number;
};

export type viewColumn = {
  id: string;
  title: string;
  order: number;
};
